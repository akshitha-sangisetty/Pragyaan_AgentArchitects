/**
 * Global Frontend Configuration for Autonomous Cloud Cost Optimizer
 * 
 * When deploying the frontend to Vercel and backend to Render:
 * Paste your Render backend URL below (e.g. "https://pragyaan-backend.onrender.com").
 * 
 * If left empty (""), the frontend will use:
 * 1. The custom URL configured in the UI Settings modal (saved in browser localStorage)
 * 2. Or relative path (/api/...) if running locally or using Vercel rewrites.
 */
window.API_BASE_URL = "https://pragyaan-agent-backend.onrender.com";
